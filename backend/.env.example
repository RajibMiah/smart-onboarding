DEBUG=""
SECRET_KEY=""
ALLOWED_HOSTS=""

# MySQL (matches docker-compose.yml at repo root)
DB_NAME=""
DB_USER=""
DB_PASSWORD=""
DB_HOST=""
DB_PORT=""

# Frontend origin allowed for CORS
CORS_ALLOWED_ORIGINS=""

# Base URL of the frontend, used to build links embedded in emails (e.g. invitations)
FRONTEND_URL=""

# Email — leave EMAIL_BACKEND unset to use the console backend (prints to the server log).
# Set these to send real email (e.g. SMTP):
EMAIL_BACKEND=""
EMAIL_HOST=""
EMAIL_PORT=""
EMAIL_HOST_USER=""
EMAIL_HOST_PASSWORD=""
EMAIL_USE_TLS=""
DEFAULT_FROM_EMAIL=""
